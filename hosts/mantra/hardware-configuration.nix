# Placeholder — replace with output of nixos-generate-config after install.
# The actual hardware scan will populate kernel modules, filesystems, etc.
# Disko handles fileSystems so no stub needed here.
{
  imports = [ ];
}
